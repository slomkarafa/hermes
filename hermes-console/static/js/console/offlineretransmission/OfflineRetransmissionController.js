var offlineRetransmission = angular.module('hermes.offlineRetransmission', []);

offlineRetransmission.controller('OfflineRetransmissionController', ['$scope',
  function ($scope) {
    $scope.daysBack = 111;
  }]);
